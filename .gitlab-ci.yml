image: ubuntu:latest

before_script:
  - source developer/ubuntu-setup.sh

tests:
  stage: test
  script:
  - cd test
  - pytest

pages:
  script:
  - cd doc
  - bash update-documentation.sh
  - cp -r html ../public
  artifacts:
    paths:
    - public
  only:
  - master
